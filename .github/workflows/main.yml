name: first workflow

on: 
  push:
    branches:
      - main

jobs:
  examble-job:
    runs-on: ubuntu-latest

    steps:
      - name: print welcome
        run: echo "welcome to first workflow lab"
